import 'package:flutter/material.dart';

class PagePlane extends StatelessWidget {
  const PagePlane({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('page_plane'),)
    );
  }
}
